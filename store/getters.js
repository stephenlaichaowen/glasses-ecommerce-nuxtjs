export const getters = {
  menu: state => state.menu,
  assort: state => state.assort,
  sellingList: state => state.sellingList,
  title: state => state.title,
  activeTabbar: state => state.activeTabbar
}